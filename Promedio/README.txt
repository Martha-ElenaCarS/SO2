
En esta carpeta encontrara el archivo:

-promedio.c: Una vez compilado Este codigo se encarga de sarcar el promedio de las 100 pruebas de cada algoritmo en el cúal se invoque 
para producir los archivos:
	-datosTiempo.dat
	-datosMemoria.dat
	
y finalmente guardarlos en el archivo que el usario decida.
